apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: sloth
  namespace: {{ .Values.sloth.namespace }}
  labels:
    app: sloth
    prometheus: prometheus
spec:
  selector:
    matchLabels:
      app: sloth
  podMetricsEndpoints:
    - port: metrics
